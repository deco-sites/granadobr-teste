import type { Props } from "../components/ui/SliderCarousel.tsx";
import Component from "../components/ui/SliderCarousel.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
